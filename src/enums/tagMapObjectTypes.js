/* eslint-disable arrow-body-style */
const enums = {
  ruleset: 'ruleset',
  supplier: 'suppliersupplier',
  supplier_group: 'SupplierGroup',
  incoming_invoice: 'incominginvoice',
  incoming_invoice_item: 'incominginvoiceitem',
};

const enumValues = Object.values(enums);

module.exports = {
  enums,
  enumValues,
};
